name: Build Obsidian Plugin

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build plugin with Windows helper
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Node dependencies
        run: npm ci

      - name: Build Rust clipboard helper
        run: npm run build:helper

      - name: Stage helper executable
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path bin | Out-Null
          Copy-Item helper\target\release\excel-html-clipboard.exe bin\excel-html-clipboard-win32-x64.exe

      - name: Build Obsidian plugin
        run: npm run build

      - name: Prepare package with helper
        shell: pwsh
        run: |
          $packageDir = "dist\excel-html-paste"
          New-Item -ItemType Directory -Force -Path "$packageDir\bin" | Out-Null
          Copy-Item manifest.json "$packageDir\manifest.json"
          Copy-Item main.js "$packageDir\main.js"
          Copy-Item styles.css "$packageDir\styles.css"
          Copy-Item bin\excel-html-clipboard-win32-x64.exe "$packageDir\bin\excel-html-clipboard-win32-x64.exe"

      - name: Prepare package without helper
        shell: pwsh
        run: |
          $packageDir = "dist\excel-html-paste-no-helper"
          New-Item -ItemType Directory -Force -Path $packageDir | Out-Null
          Copy-Item manifest.json "$packageDir\manifest.json"
          Copy-Item main.js "$packageDir\main.js"
          Copy-Item styles.css "$packageDir\styles.css"

      - name: Create release zips
        shell: pwsh
        run: |
          $version = (Get-Content manifest.json | ConvertFrom-Json).version
          Compress-Archive -Path dist\excel-html-paste\* -DestinationPath "dist\excel-html-paste-$version-with-helper.zip" -Force
          Compress-Archive -Path dist\excel-html-paste-no-helper\* -DestinationPath "dist\excel-html-paste-$version-no-helper.zip" -Force

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v4
        with:
          name: excel-html-paste-plugin
          path: |
            dist/excel-html-paste
            dist/excel-html-paste-no-helper
            dist/*.zip
          retention-days: 7

      - name: Attach plugin package to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.zip
